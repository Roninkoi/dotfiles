#!/bin/sh
input="-"
sep="[ \t+]"
if [ $# = 0 ]; then
    echo "Usage: getcol <column number> [file] [separator]"
    exit
fi
if [ $# = 2 ]; then
    input=$2
fi
if [ $# = 3 ]; then
    input=$2
    sep=$3
fi
awk -F"${sep}" '{print $'$1'}' ${input}
