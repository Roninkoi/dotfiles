#!/bin/sh
# create a single pdf file from multiple images using ImageMagick
# Usage: imgtopdf <img1.jpg> <img2.jpg> <img3.jpg>
#mogrify -format pdf $@
oldifs="$IFS"
IFS=','
infiles="$*"
IFS=$oldifs

convert "{$infiles}" -quality 100 out.pdf

